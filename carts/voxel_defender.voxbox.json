{
  "_comment": "Per-cart manifest for the bundled iso-defender cart, picked up from the <cart>.voxbox.json sidecar convention. This is what keeps engine code free of iso-defender specifics: anything the host would otherwise hardcode about one cart lives here as data.",

  "name": "iso-defender (bundled)",

  "_sfx": "Points at the authored pack rather than duplicating it into carts/, because audio/sfx.json is also the authoring source tools/sfxgen.py renders from.",
  "sfx": "/sfx.json",

  "_controls": "Shown in the controls panel. A [keys, action] pair renders the keys as a <kbd>; a bare string renders as a plain line. Host bindings (esc/p/n/m, gamepad) are listed separately and are not a cart's concern.",
  "controls": [
    ["arrows / wasd", "fly"],
    ["x", "fire"],
    ["z / c", "smart bomb"],
    ["down + z", "warp"]
  ],

  "_persistGlobals": "The cart predates any Voxatron storage API, so its README documents hiscore and level unlocks as session-only. Naming them here restores them across reloads. 'max' means never restore downward, which is what monotonic values want; a plain array would restore as saved. Carts that use cartdata/dget/dset need none of this.",
  "persistGlobals": {
    "hiscore": "max",
    "unlocked": "max"
  },

  "_camera": "Matches this cart's original Voxatron framing — tighter than the engine default, which frames the whole volume because an unknown cart may draw anywhere in it. ?cam=/?tgt=/?fov= still override.",
  "camera": {
    "pos": [64, 268, -115],
    "target": [64, 56, 40],
    "fov": 27
  },

  "_groundZ": "Deliberately absent. The engine derives the shadow plane from the volume each frame, and on this cart that lands on z=50 — the same value 01_config.lua uses — so pinning it here would only hide a regression in the derivation. Set a number to pin it, or false to turn drop shadows off.",

  "_sfx": "Omitted: the built-in cart's pack is served at /sfx.json and picked up by the sidecar convention. Other carts can put a pack URL here, or inline the whole spec.",

  "_note": "Keys starting with _ are comments; the loader ignores them."
}
